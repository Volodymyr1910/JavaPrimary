package MyMethods.arraysSort_and_camparators;

public class OddEvenComparator {

//У Ольги 2 класса, один из них имплементирует метод с другого класса. Ссылка на ГитХаб
//https://github.com/Olha011999/Olha-F-AIT-Course-2024/tree/master/lesson_29/code/homework_29/odd_even_comparator

/*

package homework_29.odd_even_comparator;

import java.util.Comparator;
import java.util.function.Function;
import java.util.function.ToDoubleFunction;
import java.util.function.ToIntFunction;
import java.util.function.ToLongFunction;

    public class OddEvenComparator implements Comparator<Integer> {



        // непосредственно метод
        @Override
        public int compare(Integer number1, Integer number2) {
            // Сравниваем четные и нечетные числа
            if (number1 % 2 == 0 && number2 % 2 != 0) {
                return -1; // number1 (четное) перед number2 (нечетным)
            } else if (number1 % 2 != 0 && number2 % 2 == 0) {
                return 1; // number1 (нечетное) после number2 (четного)
            } else {
                return 0; // Оба числа четные или оба нечетные
            }
        }

        //вызов метода
        // Sort the array using OddEvenComparator
        // Arrays.sort(numbers, new OddEvenComparator());
        // сам метод написан в классе OddEvenComparator, куда имплементирован с класса Comparable метод compare
        // Ольга в теле базового метода написала алгоритм сортировки массива сначала четные, потом нечетные


           // кампаратор работает с типом Integer, для работы с int нужно применить wrap
            Comparator <Integer> comp = new Comparator<Integer>() {
            @Override
            public int compare(Integer o1, Integer o2) {
                if (o1 % 2 == 0 && o2 % 2 != 0) {
                    return -1; // number1 (четное) перед number2 (нечетным)
                } else if (o1 % 2 != 0 && o2 % 2 == 0) {
                    return 1; // number1 (нечетное) после number2 (четного)
                } else {
                    return 0; // Оба числа четные или оба нечетные
                }//end else
            }//end compare
        };//end Comparator




*/


}//class
